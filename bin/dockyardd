#!/usr/bin/env julia

using Pkg
Pkg.activate(joinpath(@__DIR__, ".."))
using Dockyard

repo = get(ENV, "DOCKYARD_REPO", ".")
socket_path = get(ENV, "DOCKYARD_SOCKET", nothing)
Dockyard.run_daemon(repo; socket_path=socket_path)
